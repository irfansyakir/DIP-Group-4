import { Text, View } from 'react-native';

export const RadioRooms = () => {
    return (
        <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
            <Text>RadioRooms</Text>
        </View>
    );
}