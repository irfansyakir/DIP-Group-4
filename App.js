import {AppContainer} from "./src/App";
// Blue and yellow is the default color for the container element and the container element itself is black because it is
export default function App() {
  return (
    <AppContainer/>
  );
}
